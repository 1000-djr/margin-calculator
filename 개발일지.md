# 개발일지

---

## 2026-08-11 (올웨이즈 전용 버전 — 진행 중)

쿠팡만 있던 순익계산기를 올웨이즈(Alwayz)에서도 쓸 수 있게 확장. 쿠팡 코드에 합치지 않고 올웨이즈 전용 탭/테이블로 별도 구축. 궁극적으로 쿠팡+올웨이즈 통합 수익분석 목표(추후).

**설계 결정**
- 데이터: API + 엑셀 둘 다. 현재 엑셀 우선
- 판매수수료: 올웨이즈 엑셀의 '예상 정산금액(수수료 제외)'에 이미 수수료 반영 → 수수료율 계산 불필요
- 부가세: 쿠팡과 동일(면세). 매출세액 없음, 매입세액공제(수수료/11 + 광고비/11)만. 추후 과세 대비 tax_type 유지
- 순익 공식: 예상정산금액 - 원가 - 광고비 + (상품가격-예상정산금액)/11 + 광고비/11
- DB: 올웨이즈 전용 테이블(쿠팡 orders와 분리). 원가매칭은 B2B 매입가 공유

**완료: 1단계 — 주문서**
- alwayz_orders 테이블(24컬럼). POST /alwayz-orders/bulk, GET /alwayz-orders(결제일 SUBSTRING 조회)
- 탭 🌽 올웨(주문서): 엑셀 업로드 → 상품단위 필터링 모달 → 저장 → 목록. 저장은 전 컬럼, 화면은 핵심 9개
- 주문서 필터링을 쿠팡·올웨이즈 둘 다 상품단위로 개선(기존 쿠팡 주문행 단위 → 상품 그룹화)

**완료: 2-A단계 — 원가매칭(B2B 연결, 쿠팡과 공유)**
- 올웨이즈는 옵션ID 없음 → 상품아이디+옵션명 조합으로 매칭
- alwayz_product_mapping: 올웨이즈 상품 → B2B 상품 연결 브릿지. b2b_prices 매입가를 쿠팡과 공유
- GET/POST /alwayz-cost-mapping
- 원가표 별도 탭 → 주문서 탭 안으로 통합. 조회 시 상단에 B2B 미연결 상품만 노란 박스로 표시, 연결되면 사라짐

**미완성 / 다음 할 일 (흐름 유지용)**
- [ ] 2-B: 올웨이즈 수익분석. 정산금액 - B2B원가 - 광고비(아직0) + 매입세액공제. 상품별 집계
- [ ] 3단계: 올웨이즈 광고(검색광고 + 올팜광고 2종). 데이터 형태 미확인 → 광고 데이터 받아 설계
- [ ] 4단계: 발주+송장. 도매처 API 공용, 쿠팡 로직 재활용
- [ ] ★ 쿠팡 원가매칭을 주문서로 이전(일관성). 현재 쿠팡은 수익분석 탭, 올웨이즈는 주문서 탭. A안: 지금 쿠팡 건드리면 복잡한 원가로직 깨질 위험 → 보류. 나중에 쿠팡+올웨 통합 수익분석 탭 만들 때 함께 통일. 올웨이즈 주문서-내-원가연결이 새 표준 프로토타입
- [ ] 과세 상품 생길 경우 tax_type 분기(현재 전 상품 면세)

---

## 2026-08-10 (송장 수집 자동화, 신규 대형 기능)

도매처 사이트에서 송장 다운 → 쿠팡 양식 재가공 → 업로드 하던 수작업을, 도매처 API로 송장 수집 → 안심번호 매칭 → 쿠팡 송장업로드 엑셀 자동 생성으로 자동화.

**서버: 송장 수집/매칭** (POST /orders/collect-invoices, {from, to, dryRun})
- normalizePhone(p): 숫자만 추출. fetchSupplierInvoices(userId, supplierId): 도매처 API 송장 수집(커서 페이지네이션, tracking_number 있는 것만)
- 조회 기준: 결제일(order_date). SUBSTRING(order_date,1,10) 비교만(타임존 변환 금지). ordered_at/ordered_supplier_id 조건 제거 → 앱 발주분 + 도매처 직접주문분 전체 대상
- 매칭: 연동된 전체 도매처(api_linked AND api_type='adminplus')의 송장을 모아, 1차 쿠팡주문번호(customer_order_code) → 2차 안심번호(receiver_phone) 순 검색. 발주 도매처 우선 후 전체 순회
- 이미 송장 있는 주문(tracking_number 존재) → DB값으로 즉시 매칭 성공(match_by='already_done'), 엑셀 재다운로드 지원
- dryRun이면 UPDATE 안 함. matched_list에 엑셀 생성용 필드(bundle_number, product_name, option_name, option_id, quantity) 포함

**미매칭 사유 분류**
- API미연동_도매처(수동처리) / 미발송_또는_송장미등록 / 안심번호_소실(14일마스킹_과거건) / 미발송_송장대기(도매처가_아직_송장미등록)
- 안심번호 정상인데 매칭 안 됨 = 도매처가 아직 송장 미등록(미발송)으로 확인되어 '매칭실패_확인필요'에서 정정

**14일 마스킹에서 안심번호 제외** (maskOldOrders, 중요)
- 송장 매칭은 안심번호(050 가상번호) 의존인데 14일 마스킹이 전화를 덮어써 과거 발주분 매칭 불능
- UPDATE에서 recipient_phone_masked 제거 → 이름·주소·우편번호만 마스킹, 안심번호 원본 유지. placeholder \$4→\$3
- 이미 마스킹된 과거건은 원본 소실이라 복구 불가(수동 처리)

**프론트: 송장수집 서브탭** (📮 송장수집, 발주 탭 하위)
- setSupplierSubTab('invoice') 등록, initInvoiceTab()로 기간 기본값(어제~오늘 KST). 날짜 옆 '(결제일 기준)' 문구, input에 font-family:var(--sans)
- 흐름: 기간 → collectInvoices(true) 미리보기 → 매칭 테이블 + 미매칭 사유별 접기(details, 진단정보) → collectInvoices(false) 확정 → 쿠팡 엑셀 다운로드
- 매칭 카드 제목 '이미처리 X · 신규 Y' 구분

**쿠팡 송장업로드 엑셀** (downloadCoupangInvoiceExcel)
- formV4.xlsx 기준: 시트명 'form', 39컬럼(원본과 완전일치 검증)
- 필수값만: 번호, 묶음배송번호, 주문번호, 택배사, 운송장번호, 분리배송 Y/N(기본 N), 옵션ID(index 14). 나머지 34개 빈값
- 택배사명 변환 COURIER_MAP: 우체국택배→우체국, 로젠택배→로젠택배, CJ대한통운→'CJ 대한통운'(띄어쓰기 주의), 롯데/한진/경동 등. 미매핑 시 원본+alert
- 파일명: 쿠팡송장업로드_YYYYMMDD.xlsx

**검증 결과**
- 안심번호 형식 일치, 페이지네이션 정상(전체 약 3282건 수집). 매칭 로직 자체 정상 — 오늘 직접발주분 미매칭은 도매처 송장 미등록(미발송)이 원인
- customer_order_code 현재 전 도매처 빈 값 → 매칭 사실상 안심번호 100% 의존
- 진단용 임시 엔드포인트(debug-invoice-phones, adminplus-invoice-test) 제거

**검증 필요 (나중)**
- [ ] 여러 일자 합산 업로드 오류: 단건은 무수정 정상, 여러 날짜 합치면 쿠팡 오류(사유 미기록). 송장 쌓인 후 재현 → 원인 수정(중복 주문번호/이미 처리분/미발송 빈송장/분리배송 다중송장 추정)
- [ ] 분리배송(한 주문 다중 송장) 발생 여부 확인 후 처리(현재 전건 N)

---

## 2026-06~08 (6/6 이후 누적 정리)

### 쿠폰 / 상시할인가 타입 구분
- 쿠폰을 즉시할인 / 다운로드 타입으로 구분 처리
- 상시할인가 반영 로직 정비

### 타임존 이중변환 버그 수정 (중요)
- order_date가 KST 벽시계 문자열인데 TO_TIMESTAMP 후 AT TIME ZONE으로 이중변환되던 버그
- 할인 날짜 매칭이 하루 밀리던 문제 해결
- 원칙: order_date는 SUBSTRING만 사용, AT TIME ZONE 변환 금지

### PNM 옵션ID 공존 (아이템위너 대응, 중요)
- product_name_mapping UNIQUE가 (user_id,registered_name,option_name)이라 같은 상품+옵션명에 option_id가 다른 두 옵션이 공존 못하던 문제
- option_id 컬럼 추가 + UNIQUE를 (user_id,registered_name,option_name,COALESCE(option_id,'')) 로 변경
- 아이템위너로 한 옵션이 여러 option_id 갖는 경우 매칭 정상화
- 수익분석 상품별수익에서 동일 옵션명 두 option_id를 한 줄로 합산

### 2단 그룹핑 (계산이력 / B2B매입가 / 상품별수익)
- 여러 화면에서 항목을 상위 그룹으로 묶어 보여주는 2단 구조 적용

### 광고 심층분석 3단계 (신규 대형 기능)
- 광고 탭에 '광고 심층분석' 진입, 기간 토글(어제/7일/이번달, KST offset 방식), 전환기준 토글(전체/1일/14일), 순광고비(VAT제외) 기준
- 1단계 옵션별: 손익분기 ROAS 계산(마진율=쿠폰전매출 기준), 상태 3-way(정상/판매없음/원가매칭필요), 원가매칭 바로가기
- 2단계 키워드별: ROAS 절대값 + 제외후보(주문0+클릭≥임계값)/타겟후보 판정, 클릭 임계값 조절, 캠페인별 그룹핑, 빈키워드(-) 제외, 전환0건만 필터, 엑셀 다운로드
- 3단계 노출영역별: 검색/비검색/리타겟팅 3영역 카드+테이블 비교, 비중/ROAS

### 팀 기능 (계정 공유)
- 한 셀러 계정을 직원 여러 명이 나눠 쓰는 계정 공유 기능
- account_shares 테이블, 이메일 초대(미가입자도 로그인 시 자동 연결), 계정 전환 드롭다운
- requireRealAdmin 보안 강화(공유 멤버가 어드민 권한 못 얻게)

### DB 마이그레이션 순서 버그 수정 (중요, 양쪽 저장소)
- initDB에서 ALTER TABLE ad_reports가 CREATE TABLE ad_reports보다 먼저 실행돼 신규 DB 배포 시 연쇄 실패 → 구글 로그인 붕괴하던 시한폭탄
- ad_reports ALTER를 CREATE 이후로 이동 + 각 마이그레이션 블록 try/catch 격리
- users 마이그레이션은 단독 await + try/catch 없이 유지(로그인 필수라 실패 시 명시적 에러)
- 과일용(margin-calculator) + 그로스용(margin-calculator-dev) 양쪽 적용

### 감자·고구마 수수료율 별도 적용
- 기본 수수료 11.66%(10.6%+VAT), 감자·고구마는 8.36%(7.6%+VAT)
- profit.js order_detail CTE에 line_commission 컬럼(상품명 LIKE 감자/고구마면 0.0836, 아니면 0.1166), 3곳(summary/기간별/상품별) SUM(line_commission)으로 합산
- 주의: 상품명 키워드 방식이라 가공품(감자칩 등) 오탐 가능

### 원가매칭 첫 주문일 버그 수정
- 서버 페이지네이션으로 클라이언트 orders가 비어 첫 주문일을 조회시작일로 잘못 대체하던 문제
- GET /orders/option-first-date 엔드포인트로 서버에서 실제 첫 주문일 조회, 못 찾으면 라벨을 '조회 시작일'로 정직하게 표시

### 도매처 API 연동 (어드민플러스, 신규 대형 기능)
- 크롤링 대신 어드민플러스 Open API(OAuth2 client_credentials) 연동. 도매처별 서브도메인=별도 거래처=별도 키
- wholesale_suppliers에 api_type/api_client_id/api_client_secret_enc/api_linked 컬럼 추가, secret은 AES-256-GCM 암호화 저장(ENCRYPTION_KEY 환경변수), 응답 마스킹
- 도매처 관리에서 API 연동 등록 + 연결 테스트. api_type으로 향후 발주오라 등 확장 대비
- Railway static outbound IP 3개 화이트리스트 등록
- 도매처 탭: 상품 보기(도매처 선택 드롭다운) + 가격 비교 하위탭
- 가격 비교: 여러 도매처 상품을 검색, kg별 카드 박스로 그룹핑, 박스 안 2열 배치(열우선 세로 가나다순), +B2B매입가 저장 연동(시작일 선택)

### 도매처 상품 DB 캐시 + 수동 동기화
- supplier_products 테이블 추가 (도매처 상품 캐시: product_code, name, price, taxable, image, stock, unit 등)
- 기존엔 검색할 때마다 모든 도매처 API를 실시간 호출해 느렸음 → DB 캐시 방식으로 전환, 검색이 DB ILIKE라 즉시 응답
- 수동 '동기화' 버튼: 연동 도매처 전체 상품을 API로 받아 DB upsert. 마지막 동기화 시각 표시
- 서버측 단위 추출 함수(extractUnit) 추가 (kg/g/개/봉 등)

### 도매처 자동 동기화 스케줄러
- scheduler.js: node-cron으로 매일 04:00, 16:00 KST 자동 동기화
- syncSupplierProductsForUser 함수로 분리해 수동/자동 공용
- 실제 04:00 자동 실행 확인 (더그린 476 + 늘푸른우리 718 + 에코앤팜 886 = 2080건 동기화 성공)

### 도매처 가격 변동 이력
- supplier_price_history 테이블 (old_price, new_price, changed_at)
- 동기화 시 기존 가격과 다르면 자동 기록 (신규 상품은 제외, 사라진 상품은 정리)
- 가격비교 화면에 '📈 최근 가격 변동' 접이식 섹션 (7일/30일 필터, 오르면 빨강▲ 내리면 초록▼)

---

## 2026-06-06

### 🔥 핵심 버그 수정: 상시할인가/쿠폰 날짜매칭 타임존 이중변환
- **증상**: 같은 옵션·같은 할인인데 6/4 주문은 6100원 할인, 6/6 주문은 3000원만 적용되는 등 시각에 따라 할인 매칭이 들쭉날쭉
- **원인**: profit.js에서 order_date(KST 벽시계 문자열)를 `TO_TIMESTAMP(...) AT TIME ZONE 'Asia/Seoul'`로 변환할 때, `TO_TIMESTAMP`가 이미 timestamptz를 반환하는데 거기에 또 `AT TIME ZONE`을 붙여 ±9시간이 이중 적용 → 주문 시각이 18시간 틀어짐 → 상시할인가/쿠폰 기간 매칭 어긋남
- **해결**: `TO_TIMESTAMP(...)::timestamp AT TIME ZONE 'Asia/Seoul'`로 수정 (naive 캐스팅 후 KST 해석). THEN절 4곳 수정, ELSE절(`TO_DATE::TIMESTAMP`)은 이미 정상이라 미변경
- **영향**: 시각이 찍힌 전 계정의 모든 주문 할인 계산이 그동안 부정확했던 것을 바로잡음. 수익분석 정확도 전반 개선
- 관련 커밋: `75832ee`

### 데이터 정합성 / 버그 수정
- **계정 간 데이터 오염 진단·삭제 (어드민)**: cross-user-check, fix-ad-reports. B계정 광고데이터가 A계정 user_id로 잘못 저장된 건 진단/정리
- **대용량 계정 주문 빈화면**: 전체 주문을 localStorage에 한번에 로딩하던 것을 서버 페이지네이션으로 전환 (2600건+ 계정에서 빈화면 발생하던 문제)
- **b2b 매입가 빈화면**: 구버전 b2bProducts JSONB의 prices undefined 항목에서 TypeError → loadUserData 전체가 catch로 빠져 b2b-prices 로딩 실패. prices 가드 추가 + b2b-prices 우선 로딩
- **빈 날짜 광고데이터 정리**: 날짜 열 누락 업로드로 report_date 공란 138건 → 수익분석 누락. 어드민에 empty-date-ads 조회/채우기/삭제 기능 추가
- **B2B 드롭다운 옵션ID 오염**: 마진계산 'B2B에서 불러오기'가 구버전 b2bProducts(오염된 옵션ID 포함)를 쓰던 것을 B2B매입가 탭과 동일한 b2bPriceHistory 소스로 통일. 공급처 버튼 동적 생성으로 변경

### 기능 추가
- **주문 출고중지(cancel) 타입**: 일괄 미인식 모달 + 상단 필터 + 선택 액션바에 버튼 추가, 버튼 선택 시 색상 반전
- **수익분석 광고-only 행 상품명/옵션명 컬럼 분리**: 주문 행과 동일 포맷, 옵션값 중복 제거
- **수익분석 광고비 비중 카드 추가**: ad_cost_raw / 쿠폰적용매출, 부가세 미포함 광고비 기준
- **마진계산 E-ROAS(손익분기 ROAS) 카드 추가**: 1/마진율, %표시
- **어드민 진단 섹션 너비 통일** (diag-card 1160px) + **전체 앱 폭 1080→1280px 확대**

### 플랫폼 확장 (올웨이즈/테무/토스 대비)
- **1단계 완료**: orders/ad_reports/coupons/returns에 platform 컬럼 추가(기본 coupang), 기존 데이터 coupang 백필, 인덱스 추가
- **설계 방향**: 저장구조는 통일(platform 컬럼), 입력 어댑터만 플랫폼별로. 화면은 플랫폼 1급 탭 구조(쿠팡/올웨이즈/테무 각각 주문서·광고·쿠폰). 공용 탭은 마진계산·B2B매입가·수익분석
- **남은 단계**: 2단계(화면 플랫폼 탭 재편), 3단계(올웨이즈 엑셀 파서), API 연동은 플랫폼별 가능한 것만 추후

### 쿠폰/상시할인가 타입 구분 (즉시할인/다운로드)
- **coupons**: `coupon_type` 컬럼 추가(기본 instant), 기존 전부 instant 백필. 한 옵션에 즉시할인 최대1개 + 다운로드 최대1개 합산 집계로 변경(기존엔 최대 1개만 반영). 등록폼 타입선택/엑셀양식/목록 배지 추가
- **fixed_discounts**: `discount_type` 컬럼 추가, UNIQUE 제약을 `(user_id, option_id, start_date, discount_type)`으로 변경, autoCloseActiveDiscounts에 타입 조건 추가(다운로드 등록 시 즉시할인이 종료되지 않도록), 타입별 합산 집계. 등록폼/엑셀/목록 동일 적용

### 다음 할 일
- [ ] 임시 진단 엔드포인트 정리: discount-debug, discount-match (디버깅용으로 추가한 것)
- [ ] 수익분석 반품비용 차감 숫자 검증 (코드는 반영됨)
- [ ] 경북 부사 꿀사과 오염 광고데이터 6건 삭제 여부 확인
- [ ] 플랫폼 확장 2·3단계 (올웨이즈 엑셀 샘플 확보됨)
- [ ] 기존 숙제: 쿠폰 API 검증, 트래픽관리 탭, Wing 정산 비교, 사용자별 쿠팡 API 연동

---

## 2026-05-23

### DB 테이블 구조 개편
- 기존 localStorage/JSONB 방식 → 전용 테이블로 분리
- 추가된 테이블: orders, ad_reports, coupons, shortcuts, cost_mapping
- 추가된 테이블: suppliers, b2b_products, b2b_prices, calc_history, platforms
- 로그인 시 어느 기기에서든 동일 데이터 접근 가능

### 주문서 탭
- 쿠팡 주문서 엑셀 업로드 구현
- 마스킹 컬럼: 구매자, 구매자전화번호, 수취인이름, 수취인전화번호, 우편번호, 수취인주소
- 마스킹 방식: 이름(홍*동), 전화번호(010-****-1234), 주소(시/도+시/군/구만 표시)
- 실제 컬럼명 수정: '구매수(수량)', '옵션판매가(판매단가)', '노출상품명(옵션명)', '수취인 주소'
- 중복 업로드 방지: 주문번호 기준 중복 체크, 일부 중복 시 신규건만 업로드 선택 가능

### 광고 탭
- 쿠팡 광고보고서 엑셀 업로드 구현
- 실광고비 = 광고비 × 1.1 (부가세 별도)
- 원본 데이터 전체 저장 (raw_data JSONB + 44개 컬럼 개별)
- 그룹핑: 광고집행 상품명 첫 번째 ',' 이전 텍스트 기준 (가독성)
- 실제 매핑: 광고집행 옵션ID ↔ 주문서 노출상품ID 기준 (정확성)
- 상품 매칭 섹션: 광고 옵션ID ↔ 노출상품ID 연결 UI
- 중복 업로드 방지: 날짜+옵션ID+키워드 조합 기준

### 쿠폰 탭
- 쿠폰명, 할인금액, 적용기간, 적용상품 등록
- 주문서 데이터 연동 → 실매출 = (결제액 + 배송비) - 쿠폰할인액

### 수익분석 탭
- 순이익 = 실매출 - 쿠팡수수료 - 원가 - 실광고비 - 부가세납부액
- 실매출 = (결제액 + 배송비) - 쿠폰할인액
- 쿠팡수수료 = 실매출 × 수수료율
- 원가 = 공급가 × 수량 (주문일 기준 B2B 매입가 적용)
- 실광고비 = 광고비 × 1.1
- 부가세납부액:
  - 면세(디폴트): (쿠팡수수료/11) + (광고비/11)
  - 과세: (실매출/11) - (원가/11) - (쿠팡수수료/11) - (광고비/11)
- 원가 매칭: 옵션ID 정확 일치, 미매칭 시 수동 지정, 유사매칭 없음
- 매일 업로드 시 이전 공급가 확인 팝업 → 변경 시 B2B 탭 자동 저장

### 확정된 수식 (반드시 검토 후 구현)
- 실매출 = 결제액 + 배송비 - 쿠폰할인액
- 실광고비 = 광고비 × 1.1
- 쿠팡수수료 = 실매출 × 수수료율
- 부가세(면세): (쿠팡수수료 + 광고비) / 11
- 부가세(과세): (실매출 - 원가 - 쿠팡수수료 - 광고비) / 11
- 순이익 = 실매출 - 쿠팡수수료 - 원가 - 실광고비 - 부가세납부액

### 다음 할 일
- [ ] 수익분석 탭 실제 데이터 연동 테스트
- [ ] 광고성과분석 탭 (키워드별, 지면별, 옵션별 분석)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] B2B 매입가/공급처/계산이력/판매매체 DB 마이그레이션 완료 확인

---

## 2026-05-23 (오후)

### 쿠폰 탭 개편
- 수동 입력 방식 (쿠팡 쿠폰 다운로드 불가)
- 입력 항목: 쿠폰명, 쿠폰ID, 할인금액, 사용기간(시작~종료), 적용 옵션ID
- 옵션ID 여러 개 입력: 쉼표 또는 엔터로 구분 → 자동 파싱
- 적용 로직: 주문일시가 쿠폰 기간 내 + 옵션ID 일치 시 전체 주문건에 동일 할인 적용
- 실시간 건수 반영 없음 (보수적 관리)
- 대량 등록: 엑셀 양식 다운로드 + 업로드로 일괄 등록
- 중복 체크: 쿠폰ID 기준

### B2B 매입가 탭 전면 개편
- 날짜 기간 기준 공급처+공급가 이력 관리
- 케이스 1: 동일 상품, 공급처 변경 (예: ~5/13 늘푸른우리 → 5/14~ 팜보스)
- 케이스 2: 동일 상품, 동일 공급처, 공급가 변경 (예: ~5/18 A원가 → 5/19~ B원가)
- 이력 입력 항목: 상품명, 공급처, 공급가, 시작일, 종료일(미입력 시 진행중)
- 현재 적용중인 공급처/공급가 초록 강조
- 엑셀 대량 입력: 양식 다운로드 + 업로드
- 수익분석 연동: 주문일 기준 start_date <= 주문일 <= end_date 공급가 자동 적용
- 미매칭 시 수동 지정

### 탭별 URL 해시 라우팅
- 각 탭 클릭 시 URL 해시 변경
- #margin, #b2b, #history, #shortcuts, #settings, #orders, #ads, #coupons, #analytics
- 페이지 로드 시 해시 읽어서 해당 탭 자동 활성화
- 브라우저 뒤로가기/앞으로가기 탭 이동 지원

### 다음 할 일
- [ ] 실 데이터 넣어서 수익분석 탭 테스트
- [ ] 광고성과분석 탭 (키워드별, 지면별, 옵션별)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 쿠폰 대량 등록 테스트
- [ ] B2B 매입가 이력 관리 테스트

---

## 2026-05-24 (새벽)

### 핵심 원칙 (절대 준수)
- 수식은 반드시 먼저 제안 → 검토 후 구현 (임의 적용 금지)
- 로우데이터는 파싱 성공/실패 관계없이 raw_data에 무조건 전체 저장
- 로우데이터는 하나도 누락 없이 모든 컬럼 저장

### 수익분석 수식 확정
- 실매출 = 결제액 + 배송비 - 쿠폰할인액
- 쿠팡수수료 = 실매출 × 수수료율
- 원가 = 공급가 × 수량 (주문일 기준 B2B 매입가 이력 적용)
- 실광고비 = 광고비 × 1.1
- 면세 부가세 = -(수수료/11) - (광고비/11) → 음수로 표시
- 과세 부가세 = (실매출/11) - (원가/11) - (수수료/11) - (광고비/11)
- 순이익 = 실매출 - 수수료 - 원가 - 실광고비 - 부가세
- 미인식 주문건은 모든 계산에서 완전 제외

### 주문서 탭
- 날짜 컬럼 파싱 수정 (엑셀 시리얼 넘버 → YYYY-MM-DD HH:mm)
- 인식여부 토글 UI (초록=인식, 회색=미인식)
- 주문번호 일괄 미인식 기능
- 컬럼 설정 (표시/숨김 선택, 전체 데이터는 유지)
- 쿠폰적용매출 컬럼 추가
- 413 오류 수정 (서버 요청 크기 100mb, 청크 500건)

### 광고 탭
- 날짜 파싱 수정 (20260501 정수형 → 2026-05-01)
- raw_data 무조건 저장 (파싱 실패해도 저장 중단 금지)
- 광고보고서 DB 저장 안 되던 문제 수정 후 재업로드 필요

### 수익분석 탭
- 원가 매칭: 등록상품명+옵션명 → B2B상품명+단위 매핑 (한번 저장 후 자동 적용)
- 주문일 기준 B2B 매입가 이력에서 공급가 자동 적용
- product_name_mapping 테이블 추가
- 기간별 집계: 매출(쿠폰전) / 매출(쿠폰후) 분리 표시
- 광고비 기간별 집계 연동 작업 중

### B2B 매입가
- 날짜 기간 기준 공급처+공급가 이력 관리
- 엑셀 대량 업로드 (종료일 선택사항으로 표시)

### UI 개선
- 헤더 쿠팡 11.66% 배지 제거
- 컬럼 설정 모달 흰 배경으로 변경
- 탭별 URL 해시 라우팅 수정 중

### 다음 할 일
- [ ] 광고보고서 재업로드 후 기간별 집계 광고비 확인
- [ ] 기간별 집계 광고비 연동 최종 확인
- [ ] 수익분석 전체 수식 검증
- [ ] adminplus API 연동 (연내 제공 예정)

---

## 2026-05-24 (오후)

### 수익분석 탭
- 요약 카드 순서 확정: 총판매건수, 총매출(쿠폰전), 쿠폰적용매출, 순수익, 순수익률 / 총원가, 총쿠폰할인, 총광고비, 총부가세, ROAS
- ROAS = 쿠폰적용매출 / 실광고비 × 100 (검토 완료)
- 순수익률 = 순이익 / 쿠폰적용매출 × 100 (검토 완료)
- 기간별 집계에 매출(쿠폰전)/매출(쿠폰후) 분리 표시
- 기간별 집계에 판매건수 컬럼 추가
- 원가 매칭: 등록상품명+옵션명 → B2B상품명+단위만 연결 (공급처/공급가는 B2B 매입가 탭 이력에서 자동 적용)
- 원가 이력 날짜 기준: 분석 기간 시작일 아닌 해당 상품 실제 첫 주문일 기준으로 수정
- 빠른 기간 선택 버튼 추가: 어제 / 최근7일 / 이번달

### 주문서 탭
- 구매확정일자 날짜 형식 통일 (YYYY-MM-DD HH:mm)
- 안내 버튼 추가: 쿠팡 Wing → 주문/배송 → 배송 관리에서 다운로드 안내
- 개인정보 자동 마스킹 안내 문구
- 안내 이미지: public/배송관리.png

### 광고 탭
- 안내 버튼 추가: 쿠팡 Wing → 광고센터 → 광고보고서 다운로드 설정 안내
- 안내 이미지: public/guide-ad-report.png
- 일자별 광고비 요약 테이블 추가 (날짜+요일, 광고비, 실광고비, 노출수, 클릭수)
- 빠른 기간 선택 버튼 추가: 어제 / 최근7일 / 이번달

### 쿠폰 탭
- 안내 버튼 추가: 쿠팡 Wing → 프로모션 → 할인쿠폰 관리 안내
- 다운로드 불가 안내 문구 포함
- 안내 이미지: public/coupon.png

### 탭 구조 개편
- 계산 이력 → 마진 계산 탭 내부로 이동
- 바로가기 → 헤더 프로필 옆 드롭다운으로 이동
- 최종 탭: 마진계산 / B2B매입가 / 설정 / 주문서 / 광고 / 쿠폰 / 수익분석

### UI 개선
- 컬럼 설정 모달 흰 배경으로 수정
- 안내 모달 이미지 크기 조정 (주문서 60%)
- 광고보고서 날짜 파싱 수정 (정수형 20260501 → 2026-05-01)
- raw_data 무조건 저장 원칙 적용

### 다음 할 일
- [ ] 광고보고서 재업로드 후 기간별 집계 광고비 확인
- [ ] 수익분석 전체 수식 실데이터 검증
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 탭 URL 해시 라우팅 최종 확인

---

## 2026-05-25 (새벽)

### 핵심 원칙 재확인
- 수식은 반드시 먼저 제안 → 검토 후 구현
- 로우데이터는 파싱 성공/실패 관계없이 raw_data에 무조건 전체 저장
- 계산 로직은 백엔드 한 곳(api.js calculateProfit)으로 통일 → 프론트/어드민 동일 값 보장

### 어드민 페이지
- 유저별 매출(쿠폰전/후), 광고비(실), 순이익, 마진율 표시
- 날짜 필터: 어제/최근7일/이번달/전체 + 직접 입력
- 날짜 계산 브라우저 로컬 시간(한국) 기준으로 수정
- 유저 승인/차단/만료일 관리
- 어드민 권한 부여/해제
- Pretendard 폰트 적용
- 금액 표시 ₩ → 원 으로 변경
- 어드민 버튼 헤더에 추가 (is_admin=true 유저만 표시)

### 광고 탭
- 노출지면(ad_placement) 저장 문제 해결
  - 컬럼명 '광고 노출 지면' 정확히 매핑
  - INSERT 쿼리에 ad_placement 포함 확인
  - 기존 데이터 raw_data에서 UPDATE
- 중복 체크 조건 변경: user_id + report_date + campaign_id + option_id + keyword + ad_placement
- 로우데이터 전체 내역 테이블 맨 아래 추가 (접기/펼치기)
- 일자별 광고비 요약 테이블 추가
- 빠른 기간 선택: 어제/최근7일/이번달
- 안내 버튼 추가 (다운로드 방법 이미지 포함)

### 수익분석 탭
- 계산 로직 백엔드 통일 (calculateProfit 함수)
- 어드민과 수익분석 탭 동일한 값 출력 확인
- 원가 매칭: 등록상품명+옵션명 → B2B상품명+단위 (공급처는 B2B 이력에서 자동)
- 원가 이력 기준: 해당 상품 실제 첫 주문일 기준
- 요약 카드: 총판매건수/총매출(쿠폰전)/쿠폰적용매출/순이익/순수익률/총원가/총쿠폰할인/총광고비/총부가세/ROAS

### 주문서 탭
- 구매확정일자 날짜 형식 통일
- 안내 버튼 추가

### 쿠폰 탭
- 안내 버튼 추가 (coupon.png 이미지)
- 쿠폰 중복 적용 방지: 할인금액 큰 쿠폰 1개만 적용

### 탭 구조
- 설정 탭 제거 → 마진계산 탭 내 "매체 관리" 버튼으로 이동
- 최종 탭: 마진계산 / B2B매입가 / 주문서 / 광고 / 쿠폰 / 수익분석

### 다음 할 일
- [ ] 반품 관리 탭 추가
- [ ] URL 해시 라우팅 최종 확인
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 수익분석 실데이터 전체 검증

---

## 2026-05-25 (오후)

### 반품 관리 탭 추가
- 쿠팡 반품 엑셀 업로드 (returnDelivery 파일)
- 컬럼: 접수일, 접수번호, 노출상품명, 옵션, 수량, 반품사유, 반품배송비, 배송비부담, 환불예정금액, 주문번호, 옵션ID
- 마스킹: 수취인, 연락처, 반품지, 회수입고지
- 업로드 시 주문번호 기준으로 주문서 자동 미인식(반품) 처리
- 반품 처리 모달: 반품유형/비용/메모 입력
- 삭제 시 주문서 미인식 해제 옵션
- 중복 방지: 접수번호 기준

### 주문서 탭
- 미인식 유형 분리: 인식/가구매/반품/기타
- 체크박스 일괄 처리: 인식/가구매/반품/기타/삭제
- 개별 주문건 삭제 기능
- 업로드 현황: 최근 업로드 일시, 최신 주문일(시간포함)
- 주문일/등록상품명 기준 오름차순/내림차순 정렬

### 광고 탭
- 업로드 현황: 최근 업로드 일시, 최신 광고일
- 노출지면(ad_placement) 저장 완료

### 쿠폰 탭
- 날짜 입력 date+time 분리 (24시간제)
- 목록 날짜 24시간제 표시
- 사용기간 기준 오름차순/내림차순 정렬
- 요약 카드 제거

### 다음 할 일
- [ ] 반품 탭 상품명/옵션 표시 수정 완료 확인
- [ ] 수익분석에 반품비용 차감 항목 추가
- [ ] 가구매 비용 별도 분석 기능
- [ ] adminplus API 연동 (연내 제공 예정)

---

## 2026-05-25 (야간)

### 반품 탭 수정
- 반품 엑셀 업로드 시 주문서 자동 반품 미인식 처리 수정
  - orders 테이블에서 주문번호 기준으로 is_excluded=true, exclusion_type='return' 업데이트
  - orderNumbers 배열로 한번에 처리

### 다음 할 일
- [ ] 반품 업로드 시 주문서 자동 반품 처리 확인
- [ ] 수익분석에 반품비용 차감 항목 추가
- [ ] 가구매 비용 별도 분석 기능
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 새 대화에서 이어서 진행

---

## 2026-05-26

### 핵심 원칙 재확인
- 수식은 반드시 먼저 제안 → 검토 후 구현
- 로우데이터는 파싱 성공/실패 관계없이 raw_data에 무조건 전체 저장
- 계산 로직은 백엔드 api.js calculateProfit 함수로 통일

### 주문서 탭
- 필터링 후 업로드 기능 추가 (등록상품명/등록옵션명/노출상품ID/옵션ID 기준)
- 페이지네이션 추가 (10개씩/50개씩 보기, 기본값 10개)
- 로고/타이틀 클릭 시 마진계산 탭으로 이동 (홈 버튼)

### 광고 탭
- 필터링 후 업로드 기능 추가 (캠페인 기준 / 상품 기준 선택)
- 광고비 0원 행 자동 제거 (필터링 전 먼저 처리)

### B2B 매입가 탭
- 발주 상세 조정 모달: 해당 이력 공급가 기본값 자동 세팅
- 동일 상품 다른 공급가 이력 드롭다운 추가
- 진행중 이력 종료 버튼 추가 (종료일 입력)
- 발주 조정 모달 공급가 드롭다운: 공급처+공급가+기간 표시

### 쿠폰 탭
- 최근 작업일시 표시 추가
- 진행중 쿠폰 종료 기능 추가 (종료일시 입력)

### 수익분석 탭
- 등록상품명 기준 오름차순/내림차순 정렬
- 기간별 집계 날짜 기준 오름차순/내림차순 정렬
- 섹션 순서 변경: 요약카드 → 상품별수익 → 기간별집계 → 원가매칭

### 반품관리 탭
- 출고중지 엑셀 업로드 기능 추가
- 반품/출고중지 섹션 분리 (하나의 페이지에 두 섹션)
- 출고중지 처리 버튼: 출고중지완료 / 이미출고
  - 출고중지완료 → 주문서 미인식(cancel) 처리
  - 이미출고 → 반품 섹션 자동 이관
- 출고중지 처리 후 요약카드 즉시 업데이트
- db.js 마이그레이션 순서 오류 수정 (record_type 컬럼)
- 엑셀 날짜 직렬번호 파싱 수정 (cellDates:true)

### 가구매 비용 관리 (신규)
- 주문서 탭 우측 상단 "💰 가구매 비용 관리" 버튼 추가
- 업체 마스터 관리:
  - 업체명 / 진행방식(빈박스/실배송) / 리뷰유형(별점/텍스트/포토)
  - 배송비 / 배송비부가세(별도/포함) / 진행비 / 진행비부가세(별도/포함)
  - 상품원가(실배송 전용)
- 일자별 진행 기록:
  - 진행일 / 업체 선택 / 과세유형(면세/과세) 선택
  - 가구매(exclusion_type='fake') 주문건만 표시
  - 이미 처리된 주문건 음영+처리완료 배지 표시
  - 예상비용 자동 계산 (면세/과세 분기)
- 비용 계산 공식:
  - 면세: 배송비실지급 + 진행비실지급 + 쿠폰적용매출×10.66%
  - 과세: 배송비실지급 + 진행비실지급 + 쿠폰적용매출×10.66% + 부가세납부액
  - 부가세납부액 = 쿠폰적용매출/11 - 제품구매가/11 - 수수료/11 - 진행비매입세액 - 배송비매입세액
- 가구매 비용 내역: 상세보기 / 수정 / 삭제
- 수익분석 연동: "가구매 비용 포함" 토글 (ON/OFF)

### 어드민 페이지
- 계정 클릭 시 해당 계정 대리접속 기능
- 상단 "어드민 대리접속 중: {유저명}" 배너
- "원래 계정으로 돌아가기" 버튼

### 쿠팡 Open API 연동 (진행중)
- IP 화이트리스트 등록 완료 (218.39.90.42)
  - Wing에서 직접개발로 변경 후 등록
- HMAC 인증 성공, 주문 데이터 조회 확인
- 날짜 형식: yyyy-MM-dd (T00:00:00 없이)
- API 키 암호화 저장 (AES-256)
- 설정에서 API 키 등록 UI 구현
- 주문서 탭 "API로 불러오기" 버튼 구현
- 403 오류 디버깅 중:
  - 버그1: 응답 구조 오류 수정 (body.data 배열 직접 사용)
  - 버그2: pageIndex + nextToken 동시 전송 금지 수정
  - 아직 미해결 → HMAC 메시지 비교 디버깅 진행중

### 다음 할 일
- [ ] 쿠팡 API 주문 동기화 403 오류 해결
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)
- [ ] 쿠폰 API 자동 수집 연동
- [ ] 반품 부대비용 차감 (반품관리 탭 부대비용 입력 → 수익분석 차감) ←

---

## 2026-05-27 ~ 2026-05-28

### 버그 수정
- 대리접속 중 pending 계정 접속 시 어드민까지 차단되는 버그 수정
- 주문서 대량 업로드 타임아웃 문제 → 100건씩 배치 INSERT로 수정
- 쿠폰/광고/반품/출고중지 bulk INSERT 전부 배치 처리로 수정
- 쿠폰 대량 등록 날짜 직렬번호 파싱 오류 수정 (cellDates:true + KST 변환)
- 상시할인가 수정 시 새 행 추가되는 버그 수정 (UPDATE → INSERT 오류)

### 주문서 탭
- 기간별 삭제 기능 추가 (시작일~종료일 기준)

### 광고 탭
- 기간별 삭제 기능 추가 (광고집행일 기준)

### 쿠폰 탭
- 전체 삭제 버튼 추가
- 상시할인가 관리 섹션 추가 (섹션 분리: 상시할인가 / 실제쿠폰)
- 상시할인가 엑셀 대량 등록 기능 추가
- 할인 계산 방식 토글 추가 (실제쿠폰 기준 / 상시할인가 우선)
  - 선택값 DB 저장 (새로고침 유지)
- 수익분석 calculateProfit에 상시할인가 우선 적용 로직 반영
  - 주문일+옵션ID 기준 fixed_discounts 먼저 조회
  - 없으면 실제쿠폰 데이터 적용

### B2B 매입가 탭
- 면세/과세 선택 추가 (이력 등록/수정 폼)
- 엑셀 대량 입력 양식에 과세구분 컬럼 추가

### 헤더
- 로고/타이틀 클릭 시 마진계산 탭으로 이동 (홈 버튼)
- 도매처 관리 버튼 추가 (도매처명 + URL 저장/관리)

### 어드민
- 계정 클릭 시 대리접속 기능 구현
- 대리접속 중 상단 배너 표시 + 원래 계정으로 돌아가기 버튼

### 쿠팡 Open API 연동 (진행중)
- Railway Static IP 활성화 완료
  - 발급 IP: 162.220.234.240 / 162.220.234.241 / 152.55.180.240
  - Wing에 3개 IP 등록 완료
  - Static IP 적용을 위한 재배포 필요 (내일 확인 예정)
- 기존 버그 수정:
  - 응답 구조 오류 (body.data 배열 직접 사용)
  - pageIndex + nextToken 동시 전송 금지
  - loadOrders is not defined 오류 수정

### 다음 할 일
- [ ] Railway 재배포 후 쿠팡 API 주문 동기화 최종 확인
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)
- [ ] 쿠폰 API 자동 수집 연동
- [ ] 반품 부대비용 차감 (반품관리 탭 → 수익분석 차감) ← 잊지말것
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] 3번 가구매 비용 별도 분석 내용 추가 예정 (내용 전달 예정)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동 (현재는 내 계정만 테스트)
- [ ] thehans0538 계정 주문 2000건 업로드 재시도 확인

---

## 2026-05-29 ~ 2026-05-30

### 쿠팡 Open API 연동 완료
- Railway Static IP 3개 발급 및 Wing 등록 완료
  (162.220.234.240 / 162.220.234.241 / 152.55.180.240)
- 주문 API 동기화 정상 작동
- API 동기화 후 공급가 크로스체크 기능 추가
  - 원가 미매칭 / 공급가 변동 감지 자동 알림
- API 주문건 옵션ID 누락 문제 수정 (vendorItemId 매핑)
- API 주문건 주문일시 초단위까지 저장
- 최근 동기화 시간 표시 추가
- 반품/출고중지 API 연동 구현 (검증 필요)

### 수익분석
- 상품별 수익 GROUP BY option_id 기준으로 통일
- 상시할인가 우선 적용 시 쿠폰할인 반영 수정

### 주문서 탭
- 빠른 날짜 선택 버튼 추가 (어제/최근7일/이번달)
- 엑셀 다운로드 기능 추가
- 페이지네이션 현재 페이지 강조 스타일 수정

### 쿠폰 탭
- 상시할인가 적용시작/종료일 시간(초단위)까지 입력/표시
- 상시할인가 목록에 상품명/옵션명 컬럼 추가
  (옵션ID 입력 시 자동 조회)
- 상시할인가 목록 상태 필터 추가 (전체/진행중/종료, 기본값: 진행중)
- 상시할인가 새 등록 시 동일 옵션ID 자동 종료 처리
  - bulk 등록에도 자동 종료 로직 추가
  - option_id String 변환 누락 수정
  - autoCloseActiveDiscounts 헬퍼 함수로 분리

### 서비스명 변경
- "농수산물 마진 계산기" → "농수산물 ATM"

### 다음 할 일
- [ ] 상시할인가 자동 종료 처리 제대로 되는지 확인
- [ ] 반품 API 제대로 되는지 확인
- [ ] 쿠폰 API 검증 및 구현
- [ ] 반품 부대비용 차감 (반품관리 탭 → 수익분석 차감) ← 잊지말것
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] 3번 가구매 비용 별도 분석 내용 추가 예정 (내용 전달 예정)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동 (현재는 내 계정만 테스트)
- [ ] thehans0538 계정 주문 2000건 업로드 재시도 확인
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)

---

## 2026-06-01 ~ 2026-06-02

### 버그 수정
- 반품 삭제 버튼 안 눌리는 문제 수정 (pointer-events, z-index)
- 반품 삭제 시 주문서 미인식 원복 로직 추가
- 서버 크래시 수정 (initDB try/catch 없음 + 쿠폰 UNIQUE INDEX 중복 행 충돌)
- 상시할인가 목록 화면에 안 보이는 문제 수정
  - 기본 필터 'active' → 'all' 변경
  - 쿠폰 탭 전환 시마다 최신 데이터 재로드
- 원가매칭 저장 후 새로고침 시 미매칭으로 돌아오는 버그 수정
  - saveOptionMatching API 실패 시 조용히 삼키던 문제 수정
  - renderProfit 시 product-name-mappings DB에서 재조회

### 대량 업로드 성능 개선
- localStorage 중복 체크 완전 제거 (주문서/광고/쿠폰/반품/출고중지)
- DB ON CONFLICT DO NOTHING 방식으로 중복 처리 통일
- 주문서/광고 500건씩 청크 업로드 방식으로 변경
- 주문번호 일괄 미인식 100건씩 배치 처리
- DB 인덱스 추가 (orders, ad_reports, returns)
- localStorage QuotaExceededError 근본 해결

### 수익분석 개선
- 상품별 수익 계산을 서버(profit.js)로 이관
  - 클라이언트 계산 제거 → 요약카드/기간별집계/상품별수익 합계 일치
  - 쿠폰 option_ids 타입 불일치 수정 (Number vs String)
  - override_cost_price 클라이언트 미반영 수정
  - 광고비 계산 공식 통일 (actual_ad_cost 직접 사용)
- 기간별 집계 접기/펼치기 + 페이지네이션 추가
- 상품별/기간별 엑셀 다운로드 추가
- 광고비만 있고 매출 없는 행 노란색 강조 표시

### 반품 API 컬럼 매핑 수정
- createdAt, vendorItemPackageName, vendorItemName, cancelCount
- returnShippingCharge.units, enclosePrice.units, reasonCodeText
- vendorItemId (옵션ID) 추가

### IP 문제 해결
- Wing 등록 IP 오타 수정 (155.55.180.240 → 152.55.180.240)
- 쿠팡 API 주문 동기화 정상화

### 다음 할 일
- [ ] 상시할인가 자동 종료 처리 확인
- [ ] 반품 API 정상 작동 확인
- [ ] 쿠폰 API 검증 및 구현
- [ ] 반품 부대비용 차감 (반품관리 탭 → 수익분석 차감) ← 잊지말것
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] 3번 가구매 비용 별도 분석 내용 추가 예정
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동
- [ ] thehans0538 계정 주문 2000건 업로드 재시도 확인
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)
- [ ] 수익분석 데이터 검증 (Wing 정산 데이터와 비교)
- [ ] 클론 레포(margin-calculator-dev) 최신 변경사항 머지

---

## 2026-06-02

### 주문 전체 로컬 로딩 제거 (대용량 계정 빈화면 버그 수정)
- `orders = []` — localStorage 캐시 로딩 완전 제거
- `loadUserData`에서 `safeFetch('/api/orders')` 제거 (2000건+ 계정 타임아웃/메모리 문제 해결)
- `renderOrders` 요약 카드 → `/api/orders/stats` API 호출로 교체
- `_renderedPageOrders` 모듈 변수 추가: 현재 렌더된 페이지 주문 캐시 (bulk 작업용)
- `bulkUpdateOrders`, `bulkDeleteOrders`, `deleteOrder`, `setOrderExclusionType` 등 로컬 배열 의존 코드 전면 제거
- `confirmBulkExclude` 로컬 배열 업데이트 제거 → API만 호출
- `confirmPeriodDelete` 건수 조회를 `/api/orders/stats`로 교체
- Coupang 동기화 후 `/api/orders` 재로드 블록 제거
- `getCouponAppliedOrders`, 원가매칭 productName 조회 등 `orders` 참조 → `_renderedPageOrders`로 교체
- profit.js `excluded_cnt` CTE에 `fake_order_count`, `return_count`, `other_count` 추가 → 수익분석 제외건수 카드 정상 표시

### 일괄 미인식 모달 버튼 개편
- 버튼 선택 시 배경색 반전, 미선택 시 테두리만 (`.active` 클래스 → 직접 스타일 조작)
- 색상: 인식취소 #6B7280 / 가구매 #FF9500 / 반품 #FF3B30 / 기타 #8E8E93
- `출고중지` 버튼 추가 (`exclusion_type='cancel'`, 색상 #0071E3)
- `EXCLUSION_TYPES`에 `cancel` 추가
- per-row select에도 `출고중지` 옵션 추가

### 수익분석 반품비용 차감
- profit.js `return_costs` CTE 추가: `returns` 테이블 `record_type='return'` 기간 내 `return_shipping_fee + return_cost` 합산
- 순이익 계산에 반품배송비 + 부대비용 차감
- `summary` 응답에 `total_return_shipping`, `total_return_cost` 포함
- 수익분석 요약 카드에 `총반품비용` 카드 추가 (값 0이면 미표시)

### B2B 매입가 빈화면 버그 수정
- `loadUserData`에서 `/api/b2b-prices` 로딩을 최상단으로 이동 (구버전 JSONB 처리 실패와 무관하게 보장)
- `b2bData` forEach에 `if (!Array.isArray(p.prices)) p.prices = []` 가드 추가 (TypeError 방지)

### 주문 탭 출고중지 지원
- 상단 필터 버튼에 `출고중지` 추가 (`ordersFilterCancel`)
- `setOrdersFilter` btnMap에 `cancel: 'Cancel'` 추가
- 선택 액션바에 `출고중지` 버튼 추가

### 수익분석 광고-only 행 상품명/옵션명 분리
- `cleanAdProductName` → `splitAdProductName`으로 교체
- 콤마로 이어진 광고보고서 상품명(`상품명,1개,1개,5kg,5kg`)을 `Set` 중복 제거 후
  `product_name = '상품명'`, `option_name = '5kg 1개'`로 컬럼 분리
- 광고-only 행도 주문 행과 동일한 포맷으로 표시

### 주문 요약 카드 개선
- `total_payment`, `total_shipping`: `::BIGINT` → `::NUMERIC` (문자열 반환 → 숫자 반환, 천단위 콤마 버그 수정)
- `/api/orders/stats`에 `cancel_count` 추가
- 요약 카드에 `출고중지` 카드 추가
- 모든 숫자 값에 `Number()` 래핑 (문자열 방어)
- `쿠폰적용매출(인식)` 카드 추가: profit.js와 동일한 할인 서브쿼리(fixed/coupon 모드 분기) 서버 계산
- 요약 카드 5열 고정 그리드 적용 (`summary-cards-5col` 신규 클래스, 반응형 3열/2열)

### 광고 탭 일자별 테이블 개선
- `평균CPC` 컬럼 추가: `총광고비 / 클릭수` (클릭수 0이면 0원)
- `ROAS(1일)` 컬럼 추가: `총전환매출 / 총광고비 × 100%`, ROAS ≥ 100% 녹색 / 미만 빨강

### 다음 할 일
- [ ] 반품 API 정상 작동 확인
- [ ] 쿠폰 API 검증 및 구현
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)

---

## 2026-08-05

### 도매처 예치금·적립금 잔액 조회
- `adminplusGetBalance` 헬퍼로 어드민플러스 예치금/적립금 조회 (`GET /v1/seller/balance`)
- `supplier_balances` 테이블: `UNIQUE(user_id, supplier_id)`, upsert(ON CONFLICT DO UPDATE)
- 스케줄러 자동 조회: 매일 09:00, 15:00 KST (`runAllUsersBalance`, `cron.schedule`)
- 수동 새로고침 버튼 + 도매처 탭 상단 잔액 카드(도매처색 뱃지, 예치금 강조, 50만원 미만 빨간 경고)
- 어드민 테스트 엔드포인트(`/admin/adminplus-balance-test`)로 `balance.read` 권한 확인 후 구현

### 도매처 관리 모달 → 하위탭 이동
- 헤더 🏪 클릭으로 열던 도매처 관리 모달을 도매처 탭 하위탭 `⚙️ 도매처 관리`로 이동
- 440px 폼(좌) + 목록(우) 2열 그리드. 글자·버튼 전반 확대
- 하위탭 5개: 🔍 가격비교 / 📦 상품보기 / ⚙️ 도매처관리 / 📋 발주매칭 / 🚚 발주
- `setSupplierSubTab` forEach 루프로 리팩터링
- 가격비교 kg 그룹 카드 테두리 진하게(0.5px→1.5px)

### 발주 기능 (신규 대형 기능)
목표: 쿠팡 주문서 → 도매처별 자동 분류 → 각 도매처 양식 엑셀 자동 생성. 손으로 필터 쪼개던 작업 자동화.

**발주 매칭** (`order_mappings` 테이블)
- 쿠팡 옵션 ID → 도매처 + 도매처 상품명·옵션명 1:1 매핑
- API 연동 도매처: `supplier_products` 검색(다중 단어 AND `ILIKE`, 순서 무관) → 클릭 선택
- 엑셀 발주처(랑/정다운 등, 비API): 상품명 직접 입력 UI로 분기
- `_omSupplierMap{id→객체}`로 도매처 타입 빠른 조회
- 미매칭 클릭 시 발주매칭 폼 자동채움(옵션ID/상품명), 저장 후 발주탭 자동 복귀(← 발주로 돌아가기 버튼)
- `form_key` 있는 도매처는 드롭다운에 전부 포함(API 연동 여부 무관)

**도매처 발주 양식 정의** (`orderForms.js`)
- 5개 도매처 양식: 더그린(11컬럼) / 늘푸른우리(12) / 에코앤팜(17, empty 플레이스홀더 포함) / 정다운(15) / 랑(9)
- 컬럼별 `source` 매핑: `recipient_*`, `product_name`, `option_name`, `qty`, `delivery_msg`, `sender_*`, `order_number`, `empty`
- 도매처 관리에 `form_key` 드롭다운 추가, `wholesale_suppliers` 컬럼 ALTER

**발주 탭** (`🚚 발주`)
- 보내는사람 설정(접이식, `users.sender_name/phone/address` 저장)
- 기간 프리셋 quick-pill(오늘/최근7일/이번달, 기본 오늘). KST 로컬 날짜 계산
- 미발주 주문(`ordered_at IS NULL`) → 매칭 기반 도매처별 카드 자동 분류 + 미매칭 경고 섹션
- `GET /orders/for-dispatch`: LEFT JOIN `order_mappings` + `wholesale_suppliers`, `ws.form_key` 포함

**발주서 엑셀 생성**
- 각 도매처 `form_key`의 `ORDER_FORMS` 컬럼 순서대로 SheetJS `aoa_to_sheet` 생성
- 개별 다운로드: `{도매처명}_발주_{날짜}.xlsx`
- 전체 다운로드: JSZip(CDN)으로 여러 도매처 묶어 `.zip`
- 다운로드 성공 → `POST /orders/mark-dispatched`(ordered_at=NOW()) → 목록에서 즉시 제거
- 보내는사람 미설정 시 경고, 양식 미지정 도매처 안내

**주문 원본 저장** (발주용)
- 엑셀 업로드: 프론트에서 `maskValue` 호출 제거 → 원본 전송(화면 표시는 기존 마스킹 유지)
- API 수집: `buyerMasked → buyerOrig` 등으로 변경, `recipient_zipcode`·`delivery_msg` 추가
- 양쪽 경로 `ON CONFLICT DO UPDATE SET recipient_*=EXCLUDED.*` (재업로드 시 원본으로 갱신)
- 엑셀 경로: `COLS 25→26`, placeholder `$1~$26`. API 경로: `$1~$19`. 양쪽 검증 완료

**14일 자동 마스킹** (`scheduler.js`)
- `maskOldOrders()`: `SUBSTRING(order_date,1,10) <= cutoff` + `POSITION('*' IN recipient_name_masked)=0`(멱등성)
- 매일 03:30 KST 자동 실행. `maskName/maskPhone/maskAddr` api.js에서 export
- 발주·반품 대응 기간(14일) 유지 후 개인정보 마스킹
- [ ] 수익분석 데이터 검증 (Wing 정산 데이터와 비교)
