# 개발일지

---

## 2026-05-23

### DB 테이블 구조 개편
- 기존 localStorage/JSONB 방식 → 전용 테이블로 분리
- 추가된 테이블: orders, ad_reports, coupons, shortcuts, cost_mapping
- 추가된 테이블: suppliers, b2b_products, b2b_prices, calc_history, platforms
- 로그인 시 어느 기기에서든 동일 데이터 접근 가능

### 주문서 탭
- 쿠팡 주문서 엑셀 업로드 구현
- 마스킹 컬럼: 구매자, 구매자전화번호, 수취인이름, 수취인전화번호, 우편번호, 수취인주소
- 마스킹 방식: 이름(홍*동), 전화번호(010-****-1234), 주소(시/도+시/군/구만 표시)
- 실제 컬럼명 수정: '구매수(수량)', '옵션판매가(판매단가)', '노출상품명(옵션명)', '수취인 주소'
- 중복 업로드 방지: 주문번호 기준 중복 체크, 일부 중복 시 신규건만 업로드 선택 가능

### 광고 탭
- 쿠팡 광고보고서 엑셀 업로드 구현
- 실광고비 = 광고비 × 1.1 (부가세 별도)
- 원본 데이터 전체 저장 (raw_data JSONB + 44개 컬럼 개별)
- 그룹핑: 광고집행 상품명 첫 번째 ',' 이전 텍스트 기준 (가독성)
- 실제 매핑: 광고집행 옵션ID ↔ 주문서 노출상품ID 기준 (정확성)
- 상품 매칭 섹션: 광고 옵션ID ↔ 노출상품ID 연결 UI
- 중복 업로드 방지: 날짜+옵션ID+키워드 조합 기준

### 쿠폰 탭
- 쿠폰명, 할인금액, 적용기간, 적용상품 등록
- 주문서 데이터 연동 → 실매출 = (결제액 + 배송비) - 쿠폰할인액

### 수익분석 탭
- 순이익 = 실매출 - 쿠팡수수료 - 원가 - 실광고비 - 부가세납부액
- 실매출 = (결제액 + 배송비) - 쿠폰할인액
- 쿠팡수수료 = 실매출 × 수수료율
- 원가 = 공급가 × 수량 (주문일 기준 B2B 매입가 적용)
- 실광고비 = 광고비 × 1.1
- 부가세납부액:
  - 면세(디폴트): (쿠팡수수료/11) + (광고비/11)
  - 과세: (실매출/11) - (원가/11) - (쿠팡수수료/11) - (광고비/11)
- 원가 매칭: 옵션ID 정확 일치, 미매칭 시 수동 지정, 유사매칭 없음
- 매일 업로드 시 이전 공급가 확인 팝업 → 변경 시 B2B 탭 자동 저장

### 확정된 수식 (반드시 검토 후 구현)
- 실매출 = 결제액 + 배송비 - 쿠폰할인액
- 실광고비 = 광고비 × 1.1
- 쿠팡수수료 = 실매출 × 수수료율
- 부가세(면세): (쿠팡수수료 + 광고비) / 11
- 부가세(과세): (실매출 - 원가 - 쿠팡수수료 - 광고비) / 11
- 순이익 = 실매출 - 쿠팡수수료 - 원가 - 실광고비 - 부가세납부액

### 다음 할 일
- [ ] 수익분석 탭 실제 데이터 연동 테스트
- [ ] 광고성과분석 탭 (키워드별, 지면별, 옵션별 분석)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] B2B 매입가/공급처/계산이력/판매매체 DB 마이그레이션 완료 확인

---

## 2026-05-23 (오후)

### 쿠폰 탭 개편
- 수동 입력 방식 (쿠팡 쿠폰 다운로드 불가)
- 입력 항목: 쿠폰명, 쿠폰ID, 할인금액, 사용기간(시작~종료), 적용 옵션ID
- 옵션ID 여러 개 입력: 쉼표 또는 엔터로 구분 → 자동 파싱
- 적용 로직: 주문일시가 쿠폰 기간 내 + 옵션ID 일치 시 전체 주문건에 동일 할인 적용
- 실시간 건수 반영 없음 (보수적 관리)
- 대량 등록: 엑셀 양식 다운로드 + 업로드로 일괄 등록
- 중복 체크: 쿠폰ID 기준

### B2B 매입가 탭 전면 개편
- 날짜 기간 기준 공급처+공급가 이력 관리
- 케이스 1: 동일 상품, 공급처 변경 (예: ~5/13 늘푸른우리 → 5/14~ 팜보스)
- 케이스 2: 동일 상품, 동일 공급처, 공급가 변경 (예: ~5/18 A원가 → 5/19~ B원가)
- 이력 입력 항목: 상품명, 공급처, 공급가, 시작일, 종료일(미입력 시 진행중)
- 현재 적용중인 공급처/공급가 초록 강조
- 엑셀 대량 입력: 양식 다운로드 + 업로드
- 수익분석 연동: 주문일 기준 start_date <= 주문일 <= end_date 공급가 자동 적용
- 미매칭 시 수동 지정

### 탭별 URL 해시 라우팅
- 각 탭 클릭 시 URL 해시 변경
- #margin, #b2b, #history, #shortcuts, #settings, #orders, #ads, #coupons, #analytics
- 페이지 로드 시 해시 읽어서 해당 탭 자동 활성화
- 브라우저 뒤로가기/앞으로가기 탭 이동 지원

### 다음 할 일
- [ ] 실 데이터 넣어서 수익분석 탭 테스트
- [ ] 광고성과분석 탭 (키워드별, 지면별, 옵션별)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 쿠폰 대량 등록 테스트
- [ ] B2B 매입가 이력 관리 테스트

---

## 2026-05-24 (새벽)

### 핵심 원칙 (절대 준수)
- 수식은 반드시 먼저 제안 → 검토 후 구현 (임의 적용 금지)
- 로우데이터는 파싱 성공/실패 관계없이 raw_data에 무조건 전체 저장
- 로우데이터는 하나도 누락 없이 모든 컬럼 저장

### 수익분석 수식 확정
- 실매출 = 결제액 + 배송비 - 쿠폰할인액
- 쿠팡수수료 = 실매출 × 수수료율
- 원가 = 공급가 × 수량 (주문일 기준 B2B 매입가 이력 적용)
- 실광고비 = 광고비 × 1.1
- 면세 부가세 = -(수수료/11) - (광고비/11) → 음수로 표시
- 과세 부가세 = (실매출/11) - (원가/11) - (수수료/11) - (광고비/11)
- 순이익 = 실매출 - 수수료 - 원가 - 실광고비 - 부가세
- 미인식 주문건은 모든 계산에서 완전 제외

### 주문서 탭
- 날짜 컬럼 파싱 수정 (엑셀 시리얼 넘버 → YYYY-MM-DD HH:mm)
- 인식여부 토글 UI (초록=인식, 회색=미인식)
- 주문번호 일괄 미인식 기능
- 컬럼 설정 (표시/숨김 선택, 전체 데이터는 유지)
- 쿠폰적용매출 컬럼 추가
- 413 오류 수정 (서버 요청 크기 100mb, 청크 500건)

### 광고 탭
- 날짜 파싱 수정 (20260501 정수형 → 2026-05-01)
- raw_data 무조건 저장 (파싱 실패해도 저장 중단 금지)
- 광고보고서 DB 저장 안 되던 문제 수정 후 재업로드 필요

### 수익분석 탭
- 원가 매칭: 등록상품명+옵션명 → B2B상품명+단위 매핑 (한번 저장 후 자동 적용)
- 주문일 기준 B2B 매입가 이력에서 공급가 자동 적용
- product_name_mapping 테이블 추가
- 기간별 집계: 매출(쿠폰전) / 매출(쿠폰후) 분리 표시
- 광고비 기간별 집계 연동 작업 중

### B2B 매입가
- 날짜 기간 기준 공급처+공급가 이력 관리
- 엑셀 대량 업로드 (종료일 선택사항으로 표시)

### UI 개선
- 헤더 쿠팡 11.66% 배지 제거
- 컬럼 설정 모달 흰 배경으로 변경
- 탭별 URL 해시 라우팅 수정 중

### 다음 할 일
- [ ] 광고보고서 재업로드 후 기간별 집계 광고비 확인
- [ ] 기간별 집계 광고비 연동 최종 확인
- [ ] 수익분석 전체 수식 검증
- [ ] adminplus API 연동 (연내 제공 예정)

---

## 2026-05-24 (오후)

### 수익분석 탭
- 요약 카드 순서 확정: 총판매건수, 총매출(쿠폰전), 쿠폰적용매출, 순수익, 순수익률 / 총원가, 총쿠폰할인, 총광고비, 총부가세, ROAS
- ROAS = 쿠폰적용매출 / 실광고비 × 100 (검토 완료)
- 순수익률 = 순이익 / 쿠폰적용매출 × 100 (검토 완료)
- 기간별 집계에 매출(쿠폰전)/매출(쿠폰후) 분리 표시
- 기간별 집계에 판매건수 컬럼 추가
- 원가 매칭: 등록상품명+옵션명 → B2B상품명+단위만 연결 (공급처/공급가는 B2B 매입가 탭 이력에서 자동 적용)
- 원가 이력 날짜 기준: 분석 기간 시작일 아닌 해당 상품 실제 첫 주문일 기준으로 수정
- 빠른 기간 선택 버튼 추가: 어제 / 최근7일 / 이번달

### 주문서 탭
- 구매확정일자 날짜 형식 통일 (YYYY-MM-DD HH:mm)
- 안내 버튼 추가: 쿠팡 Wing → 주문/배송 → 배송 관리에서 다운로드 안내
- 개인정보 자동 마스킹 안내 문구
- 안내 이미지: public/배송관리.png

### 광고 탭
- 안내 버튼 추가: 쿠팡 Wing → 광고센터 → 광고보고서 다운로드 설정 안내
- 안내 이미지: public/guide-ad-report.png
- 일자별 광고비 요약 테이블 추가 (날짜+요일, 광고비, 실광고비, 노출수, 클릭수)
- 빠른 기간 선택 버튼 추가: 어제 / 최근7일 / 이번달

### 쿠폰 탭
- 안내 버튼 추가: 쿠팡 Wing → 프로모션 → 할인쿠폰 관리 안내
- 다운로드 불가 안내 문구 포함
- 안내 이미지: public/coupon.png

### 탭 구조 개편
- 계산 이력 → 마진 계산 탭 내부로 이동
- 바로가기 → 헤더 프로필 옆 드롭다운으로 이동
- 최종 탭: 마진계산 / B2B매입가 / 설정 / 주문서 / 광고 / 쿠폰 / 수익분석

### UI 개선
- 컬럼 설정 모달 흰 배경으로 수정
- 안내 모달 이미지 크기 조정 (주문서 60%)
- 광고보고서 날짜 파싱 수정 (정수형 20260501 → 2026-05-01)
- raw_data 무조건 저장 원칙 적용

### 다음 할 일
- [ ] 광고보고서 재업로드 후 기간별 집계 광고비 확인
- [ ] 수익분석 전체 수식 실데이터 검증
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 탭 URL 해시 라우팅 최종 확인

---

## 2026-05-25 (새벽)

### 핵심 원칙 재확인
- 수식은 반드시 먼저 제안 → 검토 후 구현
- 로우데이터는 파싱 성공/실패 관계없이 raw_data에 무조건 전체 저장
- 계산 로직은 백엔드 한 곳(api.js calculateProfit)으로 통일 → 프론트/어드민 동일 값 보장

### 어드민 페이지
- 유저별 매출(쿠폰전/후), 광고비(실), 순이익, 마진율 표시
- 날짜 필터: 어제/최근7일/이번달/전체 + 직접 입력
- 날짜 계산 브라우저 로컬 시간(한국) 기준으로 수정
- 유저 승인/차단/만료일 관리
- 어드민 권한 부여/해제
- Pretendard 폰트 적용
- 금액 표시 ₩ → 원 으로 변경
- 어드민 버튼 헤더에 추가 (is_admin=true 유저만 표시)

### 광고 탭
- 노출지면(ad_placement) 저장 문제 해결
  - 컬럼명 '광고 노출 지면' 정확히 매핑
  - INSERT 쿼리에 ad_placement 포함 확인
  - 기존 데이터 raw_data에서 UPDATE
- 중복 체크 조건 변경: user_id + report_date + campaign_id + option_id + keyword + ad_placement
- 로우데이터 전체 내역 테이블 맨 아래 추가 (접기/펼치기)
- 일자별 광고비 요약 테이블 추가
- 빠른 기간 선택: 어제/최근7일/이번달
- 안내 버튼 추가 (다운로드 방법 이미지 포함)

### 수익분석 탭
- 계산 로직 백엔드 통일 (calculateProfit 함수)
- 어드민과 수익분석 탭 동일한 값 출력 확인
- 원가 매칭: 등록상품명+옵션명 → B2B상품명+단위 (공급처는 B2B 이력에서 자동)
- 원가 이력 기준: 해당 상품 실제 첫 주문일 기준
- 요약 카드: 총판매건수/총매출(쿠폰전)/쿠폰적용매출/순이익/순수익률/총원가/총쿠폰할인/총광고비/총부가세/ROAS

### 주문서 탭
- 구매확정일자 날짜 형식 통일
- 안내 버튼 추가

### 쿠폰 탭
- 안내 버튼 추가 (coupon.png 이미지)
- 쿠폰 중복 적용 방지: 할인금액 큰 쿠폰 1개만 적용

### 탭 구조
- 설정 탭 제거 → 마진계산 탭 내 "매체 관리" 버튼으로 이동
- 최종 탭: 마진계산 / B2B매입가 / 주문서 / 광고 / 쿠폰 / 수익분석

### 다음 할 일
- [ ] 반품 관리 탭 추가
- [ ] URL 해시 라우팅 최종 확인
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 수익분석 실데이터 전체 검증

---

## 2026-05-25 (오후)

### 반품 관리 탭 추가
- 쿠팡 반품 엑셀 업로드 (returnDelivery 파일)
- 컬럼: 접수일, 접수번호, 노출상품명, 옵션, 수량, 반품사유, 반품배송비, 배송비부담, 환불예정금액, 주문번호, 옵션ID
- 마스킹: 수취인, 연락처, 반품지, 회수입고지
- 업로드 시 주문번호 기준으로 주문서 자동 미인식(반품) 처리
- 반품 처리 모달: 반품유형/비용/메모 입력
- 삭제 시 주문서 미인식 해제 옵션
- 중복 방지: 접수번호 기준

### 주문서 탭
- 미인식 유형 분리: 인식/가구매/반품/기타
- 체크박스 일괄 처리: 인식/가구매/반품/기타/삭제
- 개별 주문건 삭제 기능
- 업로드 현황: 최근 업로드 일시, 최신 주문일(시간포함)
- 주문일/등록상품명 기준 오름차순/내림차순 정렬

### 광고 탭
- 업로드 현황: 최근 업로드 일시, 최신 광고일
- 노출지면(ad_placement) 저장 완료

### 쿠폰 탭
- 날짜 입력 date+time 분리 (24시간제)
- 목록 날짜 24시간제 표시
- 사용기간 기준 오름차순/내림차순 정렬
- 요약 카드 제거

### 다음 할 일
- [ ] 반품 탭 상품명/옵션 표시 수정 완료 확인
- [ ] 수익분석에 반품비용 차감 항목 추가
- [ ] 가구매 비용 별도 분석 기능
- [ ] adminplus API 연동 (연내 제공 예정)

---

## 2026-05-25 (야간)

### 반품 탭 수정
- 반품 엑셀 업로드 시 주문서 자동 반품 미인식 처리 수정
  - orders 테이블에서 주문번호 기준으로 is_excluded=true, exclusion_type='return' 업데이트
  - orderNumbers 배열로 한번에 처리

### 다음 할 일
- [ ] 반품 업로드 시 주문서 자동 반품 처리 확인
- [ ] 수익분석에 반품비용 차감 항목 추가
- [ ] 가구매 비용 별도 분석 기능
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 새 대화에서 이어서 진행

---

## 2026-05-26

### 핵심 원칙 재확인
- 수식은 반드시 먼저 제안 → 검토 후 구현
- 로우데이터는 파싱 성공/실패 관계없이 raw_data에 무조건 전체 저장
- 계산 로직은 백엔드 api.js calculateProfit 함수로 통일

### 주문서 탭
- 필터링 후 업로드 기능 추가 (등록상품명/등록옵션명/노출상품ID/옵션ID 기준)
- 페이지네이션 추가 (10개씩/50개씩 보기, 기본값 10개)
- 로고/타이틀 클릭 시 마진계산 탭으로 이동 (홈 버튼)

### 광고 탭
- 필터링 후 업로드 기능 추가 (캠페인 기준 / 상품 기준 선택)
- 광고비 0원 행 자동 제거 (필터링 전 먼저 처리)

### B2B 매입가 탭
- 발주 상세 조정 모달: 해당 이력 공급가 기본값 자동 세팅
- 동일 상품 다른 공급가 이력 드롭다운 추가
- 진행중 이력 종료 버튼 추가 (종료일 입력)
- 발주 조정 모달 공급가 드롭다운: 공급처+공급가+기간 표시

### 쿠폰 탭
- 최근 작업일시 표시 추가
- 진행중 쿠폰 종료 기능 추가 (종료일시 입력)

### 수익분석 탭
- 등록상품명 기준 오름차순/내림차순 정렬
- 기간별 집계 날짜 기준 오름차순/내림차순 정렬
- 섹션 순서 변경: 요약카드 → 상품별수익 → 기간별집계 → 원가매칭

### 반품관리 탭
- 출고중지 엑셀 업로드 기능 추가
- 반품/출고중지 섹션 분리 (하나의 페이지에 두 섹션)
- 출고중지 처리 버튼: 출고중지완료 / 이미출고
  - 출고중지완료 → 주문서 미인식(cancel) 처리
  - 이미출고 → 반품 섹션 자동 이관
- 출고중지 처리 후 요약카드 즉시 업데이트
- db.js 마이그레이션 순서 오류 수정 (record_type 컬럼)
- 엑셀 날짜 직렬번호 파싱 수정 (cellDates:true)

### 가구매 비용 관리 (신규)
- 주문서 탭 우측 상단 "💰 가구매 비용 관리" 버튼 추가
- 업체 마스터 관리:
  - 업체명 / 진행방식(빈박스/실배송) / 리뷰유형(별점/텍스트/포토)
  - 배송비 / 배송비부가세(별도/포함) / 진행비 / 진행비부가세(별도/포함)
  - 상품원가(실배송 전용)
- 일자별 진행 기록:
  - 진행일 / 업체 선택 / 과세유형(면세/과세) 선택
  - 가구매(exclusion_type='fake') 주문건만 표시
  - 이미 처리된 주문건 음영+처리완료 배지 표시
  - 예상비용 자동 계산 (면세/과세 분기)
- 비용 계산 공식:
  - 면세: 배송비실지급 + 진행비실지급 + 쿠폰적용매출×10.66%
  - 과세: 배송비실지급 + 진행비실지급 + 쿠폰적용매출×10.66% + 부가세납부액
  - 부가세납부액 = 쿠폰적용매출/11 - 제품구매가/11 - 수수료/11 - 진행비매입세액 - 배송비매입세액
- 가구매 비용 내역: 상세보기 / 수정 / 삭제
- 수익분석 연동: "가구매 비용 포함" 토글 (ON/OFF)

### 어드민 페이지
- 계정 클릭 시 해당 계정 대리접속 기능
- 상단 "어드민 대리접속 중: {유저명}" 배너
- "원래 계정으로 돌아가기" 버튼

### 쿠팡 Open API 연동 (진행중)
- IP 화이트리스트 등록 완료 (218.39.90.42)
  - Wing에서 직접개발로 변경 후 등록
- HMAC 인증 성공, 주문 데이터 조회 확인
- 날짜 형식: yyyy-MM-dd (T00:00:00 없이)
- API 키 암호화 저장 (AES-256)
- 설정에서 API 키 등록 UI 구현
- 주문서 탭 "API로 불러오기" 버튼 구현
- 403 오류 디버깅 중:
  - 버그1: 응답 구조 오류 수정 (body.data 배열 직접 사용)
  - 버그2: pageIndex + nextToken 동시 전송 금지 수정
  - 아직 미해결 → HMAC 메시지 비교 디버깅 진행중

### 다음 할 일
- [ ] 쿠팡 API 주문 동기화 403 오류 해결
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)
- [ ] 쿠폰 API 자동 수집 연동
- [ ] 반품 부대비용 차감 (반품관리 탭 부대비용 입력 → 수익분석 차감) ←

---

## 2026-05-27 ~ 2026-05-28

### 버그 수정
- 대리접속 중 pending 계정 접속 시 어드민까지 차단되는 버그 수정
- 주문서 대량 업로드 타임아웃 문제 → 100건씩 배치 INSERT로 수정
- 쿠폰/광고/반품/출고중지 bulk INSERT 전부 배치 처리로 수정
- 쿠폰 대량 등록 날짜 직렬번호 파싱 오류 수정 (cellDates:true + KST 변환)
- 상시할인가 수정 시 새 행 추가되는 버그 수정 (UPDATE → INSERT 오류)

### 주문서 탭
- 기간별 삭제 기능 추가 (시작일~종료일 기준)

### 광고 탭
- 기간별 삭제 기능 추가 (광고집행일 기준)

### 쿠폰 탭
- 전체 삭제 버튼 추가
- 상시할인가 관리 섹션 추가 (섹션 분리: 상시할인가 / 실제쿠폰)
- 상시할인가 엑셀 대량 등록 기능 추가
- 할인 계산 방식 토글 추가 (실제쿠폰 기준 / 상시할인가 우선)
  - 선택값 DB 저장 (새로고침 유지)
- 수익분석 calculateProfit에 상시할인가 우선 적용 로직 반영
  - 주문일+옵션ID 기준 fixed_discounts 먼저 조회
  - 없으면 실제쿠폰 데이터 적용

### B2B 매입가 탭
- 면세/과세 선택 추가 (이력 등록/수정 폼)
- 엑셀 대량 입력 양식에 과세구분 컬럼 추가

### 헤더
- 로고/타이틀 클릭 시 마진계산 탭으로 이동 (홈 버튼)
- 도매처 관리 버튼 추가 (도매처명 + URL 저장/관리)

### 어드민
- 계정 클릭 시 대리접속 기능 구현
- 대리접속 중 상단 배너 표시 + 원래 계정으로 돌아가기 버튼

### 쿠팡 Open API 연동 (진행중)
- Railway Static IP 활성화 완료
  - 발급 IP: 162.220.234.240 / 162.220.234.241 / 152.55.180.240
  - Wing에 3개 IP 등록 완료
  - Static IP 적용을 위한 재배포 필요 (내일 확인 예정)
- 기존 버그 수정:
  - 응답 구조 오류 (body.data 배열 직접 사용)
  - pageIndex + nextToken 동시 전송 금지
  - loadOrders is not defined 오류 수정

### 다음 할 일
- [ ] Railway 재배포 후 쿠팡 API 주문 동기화 최종 확인
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)
- [ ] 쿠폰 API 자동 수집 연동
- [ ] 반품 부대비용 차감 (반품관리 탭 → 수익분석 차감) ← 잊지말것
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] 3번 가구매 비용 별도 분석 내용 추가 예정 (내용 전달 예정)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동 (현재는 내 계정만 테스트)
- [ ] thehans0538 계정 주문 2000건 업로드 재시도 확인

---

## 2026-05-29 ~ 2026-05-30

### 쿠팡 Open API 연동 완료
- Railway Static IP 3개 발급 및 Wing 등록 완료
  (162.220.234.240 / 162.220.234.241 / 152.55.180.240)
- 주문 API 동기화 정상 작동
- API 동기화 후 공급가 크로스체크 기능 추가
  - 원가 미매칭 / 공급가 변동 감지 자동 알림
- API 주문건 옵션ID 누락 문제 수정 (vendorItemId 매핑)
- API 주문건 주문일시 초단위까지 저장
- 최근 동기화 시간 표시 추가
- 반품/출고중지 API 연동 구현 (검증 필요)

### 수익분석
- 상품별 수익 GROUP BY option_id 기준으로 통일
- 상시할인가 우선 적용 시 쿠폰할인 반영 수정

### 주문서 탭
- 빠른 날짜 선택 버튼 추가 (어제/최근7일/이번달)
- 엑셀 다운로드 기능 추가
- 페이지네이션 현재 페이지 강조 스타일 수정

### 쿠폰 탭
- 상시할인가 적용시작/종료일 시간(초단위)까지 입력/표시
- 상시할인가 목록에 상품명/옵션명 컬럼 추가
  (옵션ID 입력 시 자동 조회)
- 상시할인가 목록 상태 필터 추가 (전체/진행중/종료, 기본값: 진행중)
- 상시할인가 새 등록 시 동일 옵션ID 자동 종료 처리
  - bulk 등록에도 자동 종료 로직 추가
  - option_id String 변환 누락 수정
  - autoCloseActiveDiscounts 헬퍼 함수로 분리

### 서비스명 변경
- "농수산물 마진 계산기" → "농수산물 ATM"

### 다음 할 일
- [ ] 상시할인가 자동 종료 처리 제대로 되는지 확인
- [ ] 반품 API 제대로 되는지 확인
- [ ] 쿠폰 API 검증 및 구현
- [ ] 반품 부대비용 차감 (반품관리 탭 → 수익분석 차감) ← 잊지말것
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] 3번 가구매 비용 별도 분석 내용 추가 예정 (내용 전달 예정)
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동 (현재는 내 계정만 테스트)
- [ ] thehans0538 계정 주문 2000건 업로드 재시도 확인
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)

---

## 2026-06-01 ~ 2026-06-02

### 버그 수정
- 반품 삭제 버튼 안 눌리는 문제 수정 (pointer-events, z-index)
- 반품 삭제 시 주문서 미인식 원복 로직 추가
- 서버 크래시 수정 (initDB try/catch 없음 + 쿠폰 UNIQUE INDEX 중복 행 충돌)
- 상시할인가 목록 화면에 안 보이는 문제 수정
  - 기본 필터 'active' → 'all' 변경
  - 쿠폰 탭 전환 시마다 최신 데이터 재로드
- 원가매칭 저장 후 새로고침 시 미매칭으로 돌아오는 버그 수정
  - saveOptionMatching API 실패 시 조용히 삼키던 문제 수정
  - renderProfit 시 product-name-mappings DB에서 재조회

### 대량 업로드 성능 개선
- localStorage 중복 체크 완전 제거 (주문서/광고/쿠폰/반품/출고중지)
- DB ON CONFLICT DO NOTHING 방식으로 중복 처리 통일
- 주문서/광고 500건씩 청크 업로드 방식으로 변경
- 주문번호 일괄 미인식 100건씩 배치 처리
- DB 인덱스 추가 (orders, ad_reports, returns)
- localStorage QuotaExceededError 근본 해결

### 수익분석 개선
- 상품별 수익 계산을 서버(profit.js)로 이관
  - 클라이언트 계산 제거 → 요약카드/기간별집계/상품별수익 합계 일치
  - 쿠폰 option_ids 타입 불일치 수정 (Number vs String)
  - override_cost_price 클라이언트 미반영 수정
  - 광고비 계산 공식 통일 (actual_ad_cost 직접 사용)
- 기간별 집계 접기/펼치기 + 페이지네이션 추가
- 상품별/기간별 엑셀 다운로드 추가
- 광고비만 있고 매출 없는 행 노란색 강조 표시

### 반품 API 컬럼 매핑 수정
- createdAt, vendorItemPackageName, vendorItemName, cancelCount
- returnShippingCharge.units, enclosePrice.units, reasonCodeText
- vendorItemId (옵션ID) 추가

### IP 문제 해결
- Wing 등록 IP 오타 수정 (155.55.180.240 → 152.55.180.240)
- 쿠팡 API 주문 동기화 정상화

### 다음 할 일
- [ ] 상시할인가 자동 종료 처리 확인
- [ ] 반품 API 정상 작동 확인
- [ ] 쿠폰 API 검증 및 구현
- [ ] 반품 부대비용 차감 (반품관리 탭 → 수익분석 차감) ← 잊지말것
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] 3번 가구매 비용 별도 분석 내용 추가 예정
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동
- [ ] thehans0538 계정 주문 2000건 업로드 재시도 확인
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)
- [ ] 수익분석 데이터 검증 (Wing 정산 데이터와 비교)
- [ ] 클론 레포(margin-calculator-dev) 최신 변경사항 머지

---

## 2026-06-02

### 주문 전체 로컬 로딩 제거 (대용량 계정 빈화면 버그 수정)
- `orders = []` — localStorage 캐시 로딩 완전 제거
- `loadUserData`에서 `safeFetch('/api/orders')` 제거 (2000건+ 계정 타임아웃/메모리 문제 해결)
- `renderOrders` 요약 카드 → `/api/orders/stats` API 호출로 교체
- `_renderedPageOrders` 모듈 변수 추가: 현재 렌더된 페이지 주문 캐시 (bulk 작업용)
- `bulkUpdateOrders`, `bulkDeleteOrders`, `deleteOrder`, `setOrderExclusionType` 등 로컬 배열 의존 코드 전면 제거
- `confirmBulkExclude` 로컬 배열 업데이트 제거 → API만 호출
- `confirmPeriodDelete` 건수 조회를 `/api/orders/stats`로 교체
- Coupang 동기화 후 `/api/orders` 재로드 블록 제거
- `getCouponAppliedOrders`, 원가매칭 productName 조회 등 `orders` 참조 → `_renderedPageOrders`로 교체
- profit.js `excluded_cnt` CTE에 `fake_order_count`, `return_count`, `other_count` 추가 → 수익분석 제외건수 카드 정상 표시

### 일괄 미인식 모달 버튼 개편
- 버튼 선택 시 배경색 반전, 미선택 시 테두리만 (`.active` 클래스 → 직접 스타일 조작)
- 색상: 인식취소 #6B7280 / 가구매 #FF9500 / 반품 #FF3B30 / 기타 #8E8E93
- `출고중지` 버튼 추가 (`exclusion_type='cancel'`, 색상 #0071E3)
- `EXCLUSION_TYPES`에 `cancel` 추가
- per-row select에도 `출고중지` 옵션 추가

### 수익분석 반품비용 차감
- profit.js `return_costs` CTE 추가: `returns` 테이블 `record_type='return'` 기간 내 `return_shipping_fee + return_cost` 합산
- 순이익 계산에 반품배송비 + 부대비용 차감
- `summary` 응답에 `total_return_shipping`, `total_return_cost` 포함
- 수익분석 요약 카드에 `총반품비용` 카드 추가 (값 0이면 미표시)

### B2B 매입가 빈화면 버그 수정
- `loadUserData`에서 `/api/b2b-prices` 로딩을 최상단으로 이동 (구버전 JSONB 처리 실패와 무관하게 보장)
- `b2bData` forEach에 `if (!Array.isArray(p.prices)) p.prices = []` 가드 추가 (TypeError 방지)

### 주문 탭 출고중지 지원
- 상단 필터 버튼에 `출고중지` 추가 (`ordersFilterCancel`)
- `setOrdersFilter` btnMap에 `cancel: 'Cancel'` 추가
- 선택 액션바에 `출고중지` 버튼 추가

### 수익분석 광고-only 행 상품명/옵션명 분리
- `cleanAdProductName` → `splitAdProductName`으로 교체
- 콤마로 이어진 광고보고서 상품명(`상품명,1개,1개,5kg,5kg`)을 `Set` 중복 제거 후
  `product_name = '상품명'`, `option_name = '5kg 1개'`로 컬럼 분리
- 광고-only 행도 주문 행과 동일한 포맷으로 표시

### 주문 요약 카드 개선
- `total_payment`, `total_shipping`: `::BIGINT` → `::NUMERIC` (문자열 반환 → 숫자 반환, 천단위 콤마 버그 수정)
- `/api/orders/stats`에 `cancel_count` 추가
- 요약 카드에 `출고중지` 카드 추가
- 모든 숫자 값에 `Number()` 래핑 (문자열 방어)
- `쿠폰적용매출(인식)` 카드 추가: profit.js와 동일한 할인 서브쿼리(fixed/coupon 모드 분기) 서버 계산
- 요약 카드 5열 고정 그리드 적용 (`summary-cards-5col` 신규 클래스, 반응형 3열/2열)

### 광고 탭 일자별 테이블 개선
- `평균CPC` 컬럼 추가: `총광고비 / 클릭수` (클릭수 0이면 0원)
- `ROAS(1일)` 컬럼 추가: `총전환매출 / 총광고비 × 100%`, ROAS ≥ 100% 녹색 / 미만 빨강

### 다음 할 일
- [ ] 반품 API 정상 작동 확인
- [ ] 쿠폰 API 검증 및 구현
- [ ] 가구매 비용 포함/제외 토글 수익분석 연동 최종 확인
- [ ] adminplus API 연동 (연내 제공 예정)
- [ ] 각 사용자별 쿠팡 API 연동
- [ ] 광고 API 연동 (광고보고서는 API 미제공 → 엑셀 유지)
- [ ] 수익분석 데이터 검증 (Wing 정산 데이터와 비교)
